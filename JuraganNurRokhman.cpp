#include<iostream>
using namespace std;

int main(){
    int N;
    cin >> N;
    int uang[N];
    int jumlah=0;
    for (int i=0; i<N; i++){
        cin >> uang[i];
    }
    for (int i=0; i<N; i++){
        if (uang[i] + uang[i+2] > uang)
    }
}